version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: mongo_escola
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: escola
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: